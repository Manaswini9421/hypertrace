# Dependencies for running the test suite:
#
#   pip install -r requirements-dev.txt
#   pytest -q                      # unit + integration (integration skips if
#                                  #   the cluster isn't reachable)
#   make test                      # unit only, no cluster needed
#   make integration               # integration only, opens port-forwards
#
# The unit tests stub the Kubernetes client but otherwise run against the
# real shared package, so its runtime deps have to be present. The
# integration tests additionally talk to a live RabbitMQ, TimescaleDB and
# api-bff, which is what psycopg2 and httpx are for.
pytest>=8.0
pika>=1.3
pydantic>=2.6
pydantic-settings>=2.2
SQLAlchemy>=2.0
psycopg2-binary>=2.9
httpx>=0.27
